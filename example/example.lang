

class Human {
    func init(a, b) {
       this.name = a;
       this.age  = b;
       println("SET:", this);
    }

    func getName() {
        return this.name;
    }
}

class Person extends Human {
    
}

const px = new Person("Philipp Andrew Redondo", 26);

println(px.getName(), [1,2,3]);

println("Done!");